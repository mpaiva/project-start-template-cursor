# Environment variables for MCP tools
# Replace the placeholder values with your actual secrets (never commit real secrets to public repos)

# GitHub MCP
GITHUB_PERSONAL_ACCESS_TOKEN=<YOUR_GITHUB_TOKEN>

# Perplexity MCP
PERPLEXITY_API_KEY=<YOUR_PERPLEXITY_API_KEY>

# Add additional secrets for other MCP tools as needed
